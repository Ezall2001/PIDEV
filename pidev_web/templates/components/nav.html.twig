<nav>
	<div class="container-fluid">


		<div class="row">

			<div class="col-4">
				<div class="d-flex align-items-center">

					<a href="{{path('sessions_list')}}" class="logo">
						<img src="{{asset('images/logo.png')}}">
					</a>
					<h1 class="text-light display-6 ms-4">Myalô</h1>
				</div>
			</div>

			<ul class="d-flex justify-content-center align-items-center col-4">
				<li class="mx-2">
					<a href="{{path('sessions_list')}}" class="fs-4">User</a>
				</li>
				<li class="mx-2">
					<a href="{{path('admin_sessions_list')}}" class="fs-4">Admin</a>
				</li>
				{% if (app.user) %}
							<li class="mx-2">
								<a href="{{path('app_question')}}" class="fs-4">Forum</a>
							</li>
							{% endif %}

			</ul>


			<ul class="d-flex align-items-center justify-content-end col-4">

				<li class="dropdown notification-list topbar-dropdown">
					<a class="nav-link dropdown-toggle nav-user me-0 waves-effect waves-light" data-bs-toggle="dropdown"
						href="#" role="button" aria-haspopup="false" aria-expanded="false">
						<span class="pro-user-name ms-1  text-light">
							{% if (app.user) %}
							
							{{app.user.getFirstName()}} {{app.user.getLastName()}}
							<i class="mdi mdi-chevron-down"></i>
							
							{% else %}
							<i class="fas fa-th-list"></i>
							{% endif %}
						</span>
					</a>
					<div class="dropdown-menu dropdown-menu-end profile-dropdown ">
						<div class="dropdown-header noti-title">
							<h6 class="text-overflow m-0">Bienvenue !</h6>
						</div>

						{% if (app.user) %}
						<a href="{{ path('profil') }}" class="dropdown-item notify-item">
							<i class="fe-user"></i>
							<span>Mon Profil</span>
						</a>
						<div class="dropdown-divider"></div>

						<a href="{{ path('app_logout') }}" class="dropdown-item notify-item text-danger">
							<i class="fe-log-out"></i>
							<span>Deconnexion</span>
						</a>
						{% else %}
						<a href="{{ path('app_register') }}" class="dropdown-item notify-item">
							<i class="fe-user"></i>
							<span>S'inscrire</span>
						</a>
						<div class="dropdown-divider"></div>
						<a href="{{ path('app_login') }}" class="dropdown-item notify-item text-danger">
							<i class="fe-log-out"></i>
							<span>Se Connecter</span>
						</a>
						{% endif %}
				</li>

				<li class="dropdown notification-list">
					<a href="javascript:void(0);" class="nav-link right-bar-toggle waves-effect waves-light">
						<i class="fe-settings noti-icon  text-light"></i>
					</a>
				</li>

			</ul>


		</div>
	</div>


</nav>